import type { HeroProps } from '@/types/components/Hero/index.types';

export default function Hero(props: HeroProps) {
    return (
        <div>
            <p>Hi, I&apos;m Gouthaman Kumarappan.</p>
        </div>
    );
}
